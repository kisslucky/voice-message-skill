interface:
  display_name: "Voice Message"
  short_description: "Generate and send platform-ready TTS voice messages"
  default_prompt: "Use $voice-message to turn this text reply into a voice message for the requested platform."
policy:
  allow_implicit_invocation: true
